node_modules
dist
.env
.env.local
.env.*.local
*.log
.DS_Store

# Backups de la base (datos reales / PII) — nunca al repo
/backups/
/.secrets/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Tooling local (Claude Code / graft)
/.claude/
/.ignore
/.mcp.json
